## Minimal requirements for avatar-benchmark
numpy>=1.21.0
opencv-python>=4.5.0

## Important: install PyTorch first using the official selector
## (https://pytorch.org) to match the CUDA / OS configuration, then
## run: python -m pip install -r requirements.txt
lightning>=2.0.0
wandb>=0.16.0
torchvision>=0.15.0

pillow>=8.0.0
omegaconf>=2.1.0
h5py>=3.1.0
tqdm>=4.62.0

# 3D / geometry libs used by core code (avatar templates / ply IO)
smplx
trimesh>=3.9.0
plyfile>=0.7.2
pyrender>=0.1.45

# Notes:
# - Install project-specific optional dependencies as needed for your workflows.